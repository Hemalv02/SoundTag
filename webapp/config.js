// Set this to your upload endpoint. The server should accept a
// multipart/form-data POST with fields `audio` (Blob) and
// `metadata` (JSON string). Leave null to disable upload buttons.
export const UPLOAD_ENDPOINT = null;
